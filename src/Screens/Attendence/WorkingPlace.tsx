import { StyleSheet, Text, View } from "react-native";
import React from "react";
const WorkingPlace = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
      }}
    >
      <Text>WorkingPlace!</Text>
    </View>
  );
};

export default WorkingPlace;

const styles = StyleSheet.create({});
