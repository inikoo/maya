import { StyleSheet, Text, View } from "react-native";
import React from "react";
const ClockingMachine = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
      }}
    >
      <Text>ClockingMachine!</Text>
    </View>
  );
};

export default ClockingMachine;

const styles = StyleSheet.create({});
