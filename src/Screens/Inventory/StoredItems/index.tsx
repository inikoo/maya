import React, { useLayoutEffect } from "react";
import { FlatList, Text, View, TouchableHighlight, Image } from "react-native";

export default function StoredItems(props) {
  const { navigation } = props;

  return (
    <View>
    <Text>StoredItems</Text>
    </View>
  );
}
